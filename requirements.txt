# Stark — voice assistant dependencies
google-genai>=0.3.0    # Google Gemini API (the "brain" — free tier)
vosk>=0.3.45           # offline speech-to-text + wake word
sounddevice>=0.4.6     # microphone capture
pyttsx3>=2.90          # offline text-to-speech fallback
edge-tts>=6.1.0        # free British neural voice (default Jarvis voice)
pygame>=2.5.0          # plays the synthesized mp3 audio
pyautogui>=0.9.54      # system control (volume, typing, screenshots)
Pillow>=10.0.0         # required by pyautogui for screenshots
PySide6>=6.6.0         # the Jarvis-style HUD overlay
